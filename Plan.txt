DATABASE

Bus-Stop
- name
- line(มี 3 สาย)
- latitude(เป็นค่าคงที่ไม่เปลี่ยนแปลง)
- longitude(เป็นค่าคงที่ไม่เปลี่ยนแปลง)
- density(มี 3 ระดับแต่ละระดับจะมีสีของตัวเองซึ่งแบ่งเป็น 3 สี คือ เขียว เหลือง แดง)
- passenger(จำนวนผู้โดยสารบนรถ)
- wait-time(ระยะเวลาที่รถจะมาถึง)

Bus
- name
- line(มี 3 สาย)
- latitude(มีการ Update แบบ real-time)
- longitude(มีการ Update แบบ real-time)
- density(มี 3 ระดับแต่ละระดับจะมีสีของตัวเองซึ่งแบ่งเป็น 3 สี คือ เขียว เหลือง แดง)
- passenger(จำนวนผู้โดยสารบนรถ)

FLOW-APP
- เลือกสถานที่ปลายทาง
- แอปจะทำการเลือกป้ายที่ใกล้ที่สุดให้ / บอกให้ user ไปหาป้ายที่ใกล้ที่สุด
- แสดงข้อมูลที่ป้ายนั้น + แสดงข้อมูลของรถในสาย

HARDWARE
- กล้อง + Sensor เก็บข้อมูลบนรถบัส
- กล้องเก็บข้อมูลที่ป้าย
- หลังจากได้ข้อมูลให้บันทึกลง Firebase แล้วแสดงบน Application